import { RoleTransformPipePipe } from './role-transform-pipe.pipe';

describe('RoleTransformPipePipe', () => {
  it('create an instance', () => {
    const pipe = new RoleTransformPipePipe();
    expect(pipe).toBeTruthy();
  });
});
